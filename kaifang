#include<stdio.h>
#include<math.h>
int main ()
{
	int m,n,i;
	double s;
	int a[i];
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{

	scanf("%d",&m); 
	a[i]=m;
	}
	for(i=0;i<n;i++)
	{

	if(a[i]>=0)
	{
		s=sqrt(a[i]);
		printf("%.2f\n",s);
	}
		else 
		printf("ERROR\n");
	}
	return 0;
}
